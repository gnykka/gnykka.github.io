(() => {
  window.addEventListener('load', () => {
    const main = document.getElementsByTagName('main')[0];

    // make request
    // get data
    // insert into main -> div.picture
    // pages -> on scroll ?
    // masonry
    // input fields for custom params
  });
})();